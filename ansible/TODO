general
-------

* /etc/ssh/ssh_known_host: should be created globally
* grep for `TODO`

encoder
-------

* Notify (Restart bei Changes) auch fuer runsv und sdi-script bauen
* Translated-Stream konfigurierbar machen

mixer
-----

* noch nicht initial deployed

server
------

* noch nicht initial deployed sind: storage, node, live
* router: bind Konfiguration fehlt
* router: synchronise lan.c3voc.de zone between router.lan and mngslave.dus.c3voc.de
* relays: ausrollen des frontends fehlt noch
* monitoring: munin-server
