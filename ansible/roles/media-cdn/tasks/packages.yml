---
  - name: install default packages
    apt:
      state: latest
      name:
        - zlib1g-dev
        - libgeoip-dev
        - unattended-upgrades

  - name: install default packages
    apt: name="{{ item }}" state=latest default_release=buster-backports
    loop:
      - redis-server
