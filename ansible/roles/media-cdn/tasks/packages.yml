---
  - name: install default packages
    apt: name={{ packages|flatten(levels=1) }} state=latest update_cache=yes
    tags: packages
    vars:
      packages:
      - mirrorbits
      - rsync
      - redis-server
