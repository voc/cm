---
  - import_tasks: ../../../register/bin_path.yml
  ###########
  # General #
  ###########
  # Install packages

  - name: install packages needed to monitor host
    apt:
      name:
        - nagios-plugins-basic
        - collectd
      state: latest
    tags: apt

  - import_tasks: encoder.yml
    when: ansible_hostname is match("encoder\d+")

  - import_tasks: collectd.yml
    tags: collectd

  - import_tasks: telegraf.yml
    tags: telegraf
