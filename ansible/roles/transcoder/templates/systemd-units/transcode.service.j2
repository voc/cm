#MANAGED BY ANSIBLE

[Unit]
Description = Transcoder {{ item.1 }} Stream {{ item.0 }}
Requires = transcode_{{ item.0 }}.target
After = transcode_{{ item.0 }}.target

[Service]
Type = simple
ExecStart = /opt/transcoder/scripts/{{ item.0 }}_{{ item.1 }}.sh
Restart = always
RestartSec = 30s
StartLimitInterval = 0

[Install]
WantedBy = transcode_{{ item.0 }}.target
