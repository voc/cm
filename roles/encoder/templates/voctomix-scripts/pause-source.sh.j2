#!/bin/sh
while true; do cat /opt/voc/share/pause.ts || exit 1; done |\
	ffmpeg -y -nostdin -re -i - \
	-map 0:v \
	-c:v rawvideo \
	-pix_fmt yuv420p \
	-f matroska \
	-metadata title="pause" \
	tcp://localhost:17000
