---
  - include: haproxy.yml
  - include: php.yml
  - include: sysctl.yml
  - include: iptables.yml
    when: nginx is defined and nginx == 'yes'
  - include: streaming_website.yml
    when: nginx is defined and nginx == 'yes'
  - include: nginx.yml
    when: nginx is defined and nginx == 'yes'
