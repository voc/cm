---
  # Install packages needed for each relay
  - name: install streaming relay dependencies
    apt: name={{ item }} state=present
    with_items:
      - oggfwd-voc

  # - include: relay_register.yml
  - include: nginx.yml
  - include: icecast.yml
  - include: iptables.yml
  - include: sysctl.yml
  - include: php.yml
    when: nginx_frontend_server is defined and nginx_frontend_server == 'yes'
